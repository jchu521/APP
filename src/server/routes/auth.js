const express = require("express");
const router = express.Router();
// const authController = require("../controllers/auth/auth.controller");

// //POST
// // token
// router.post("/", authController.access);

module.exports = router;
